{
  "name": "docker-image-need-update-action",
  "description": "Checks if a new version of the base image exists in the Docker registry",
  "version": "1.0.0",
  "author": "Kirill Vercetti",
  "private": true,
  "homepage": "https://github.com/kyzima-spb/docker-image-need-update-action#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kyzima-spb/docker-image-need-update-action.git"
  },
  "bugs": {
    "url": "https://github.com/kyzima-spb/docker-image-need-update-action/issues"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "Docker"
  ],
  "scripts": {
    "package": "npx ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
